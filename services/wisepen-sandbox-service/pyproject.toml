[project]
name = "wisepen-sandbox-service"
version = "0.1.0"
description = "Temporary WisePen Sandbox core lifecycle service"
requires-python = ">=3.11"
dependencies = [
    "fastapi",
    "dependency_injector",
    "httpx",
    "beanie",
    "pydantic",
    "pydantic-settings",
    "pymongo",
    "pyyaml",
    "redis[asyncio]",
    "uvicorn[standard]",
    "wisepen-common",
]

[project.optional-dependencies]
test = ["pytest", "pytest-asyncio", "httpx"]

[tool.uv.sources]
wisepen-common = { workspace = true }

[tool.pytest.ini_options]
asyncio_mode = "auto"
